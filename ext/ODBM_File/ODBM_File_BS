# NeXT needs libposix.a and libdbm.a to load along with ODBM_File.so

if ( $dlsrc eq "dl_next.xs" ) {
    @DynaLoader::dl_resolve_using = ( '/usr/lib/libposix.a', 
                                      '/usr/lib/libdbm.a' );
}
1;
